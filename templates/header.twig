<!DOCTYPE html>
<html>
    <head>
        <title>{{ title }} | {{ website_name }}</title>
        <meta charset="utf-8"/>
        <base href="/"/>
        {% if not disableRefresh %}
            <noscript>
                <meta http-equiv="refresh" content="{{ session_duration }}"/>
            </noscript>
            <script src="/ping.js"></script>
            <script>startPing({{ session_duration }});</script>
        {% endif %}
        <link href="styles.css" rel="stylesheet"/>
        <link rel="apple-touch-icon" sizes="180x180" href="/apple-touch-icon.png">
        <link rel="icon" type="image/png" sizes="32x32" href="/favicon-32x32.png">
        <link rel="icon" type="image/png" sizes="16x16" href="/favicon-16x16.png">
        <link rel="icon" href="/favicon.ico" type="image/x-icon" />
        <link rel="manifest" href="/site.webmanifest">
        <meta name="author" content="Björn 'Idrinth' Büttner" />
        <meta name="description" content="A self-made password and secret vault. Completely open source." />
    </head>
    <body>
        <div id="header">
            <a href="/"><img src="/logo.png" alt="logo"/></a>
            <strong>{{ website_name }}</strong>
            {% if logged_in %}<form method="post" onchange="this.submit();" action="/search"><input placeholder="search term" type="text" name="term" value="{{ term }}"/></form>{% endif %}
        </div>
        <div id="navigation">
            <nav>
                <ul>
                    <li><a href="/">Home</a></li>
                    {% if logged_in %}
                        <li><a href="/import">Imports</a></li>
                    {% endif %}
                    <li><a href="https://tickets.idrinth.de" target="_blank">Support</a></li>
                    {% if logged_in %}
                        <li><a href="https://paypal.me/idrinth" target="_blank">Support the Project</a></li>
                    {% endif %}
                </ul>
            </nav>
        </div>
        <div id="content">