<!DOCTYPE html>
<html>
    <head>
        <title>{{ title }} | {{ website_name }}</title>
        <meta charset="utf-8"/>
        <base href="/"/>
        <meta http-equiv="refresh" content="{{ session_duration }}">
        <link href="styles.css" type="stylesheet"/>
    </head>
    <body>
