Login added at {{ hostname }}

Hello {{ name }}!

You were given a login with the public name "{{ public }}" by {{ sender }}.
The login is available in your public folder or at https://{{ hostname }}/logins/{{ id }} .

Have a great day,
Your Walled Secrets Bot

The site {{ hostname }} is hosted and operated by {{ contact }}, see the imprint at https://{{ hostname }}/imprint for more information.